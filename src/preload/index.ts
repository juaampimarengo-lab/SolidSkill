// Intentionally empty at this checkpoint: the renderer has no privileged
// main-process bridge yet. No broker, filesystem, or credential access is
// exposed. Populate contextBridge APIs here only when a real IPC need
// exists.
export {}
